import { messagesEs } from '../i18n/messages.es';
import { commonProdEnvironment } from './environment.prod';

export const messages = messagesEs;
export const environment = Object.assign({}, commonProdEnvironment, {});
